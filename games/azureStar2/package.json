{
  "name": "azurestar2",
  "version": "1.0.0",
  "description": "A spaceship game",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile": "babel game -o azurestar2.full.js && uglifyjs -m -o azurestar2.full.js azurestar2.full.js",
    "watch-compile": "babel game --watch -o azurestar2.full.js"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.10.1",
    "babel-preset-es2015": "^6.9.0",
    "uglify-js": "^2.6.2"
  }
}
  